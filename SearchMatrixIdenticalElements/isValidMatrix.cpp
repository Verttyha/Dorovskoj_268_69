#include "functions.h"
#include <vector>


/**
 * @brief ѕровер€ет, €вл€етс€ ли заданна€ матрица корректной (не пуста€ и все строки имеют одинаковое число столбцов).
 * @param matrix ƒвумеренный вектор целых чисел, представл€ющий матрицу.
 * @return true, если матрица корректна; false Ч в противном случае.
 */

bool isValidMatrix(const std::vector<std::vector<int>>& matrix, int maxSize) {
    
    return true;  ///< ¬се проверки пройдены Ч матрица корректна.
}