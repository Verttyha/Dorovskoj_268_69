#include "functions.h"
#include "errors.h"
#include <sstream>
#include <string>
#include <vector>

/**
 * @brief ѕреобразует строки текста в матрицу целых чисел.
 *
 *  ажда€ строка должна содержать числа, разделЄнные пробелами.
 * ѕустые строки пропускаютс€.
 *
 * @param lines ¬ектор строк, кажда€ из которых представл€ет собой строку матрицы.
 * @return ƒвумеренный вектор целых чисел Ч полученна€ матрица.
 * @throws Error при неверном формате данных или если матрица не пр€моугольна€.
 */
std::vector<std::vector<int>> parseMatrixFromLines(const std::vector<std::string>& lines) {
  

    std::vector<std::vector<int>> matrix;
 
    return matrix;
}