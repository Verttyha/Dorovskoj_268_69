#include <vector>
#include <string>
#include <iostream>

std::vector<std::vector<int>> parseMatrixFromLines(const std::vector<std::string>& lines) {
    return {
        {1, 2, 3},
        {1, 2, 3},
        {1, 2, 3}
    };
}

int main() {
    return 0;
}
